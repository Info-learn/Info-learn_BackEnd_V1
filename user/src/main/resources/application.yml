spring:

  jackson:
    property-naming-strategy: SNAKE_CASE

  datasource:
    url: jdbc:mysql://localhost:3306/Infolearn
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: kang0525@

management:
  endpoints:
    web:
      exposure:
        include: refresh